[project]
name = "adderboard"
version = "0.1.0"
description = "Smallest transformer that adds two 10-digit numbers"
requires-python = ">=3.13"
dependencies = [
    "cma>=4.4.4",
    "pytest>=9.0.2",
    "rich>=14.3.3",
    "torch>=2.10.0",
    "uniplot>=0.21.5",
]

[tool.uv]
package = false
