# Session Log: 2026-02-28

Last updated: 2026-02-28

## Goal

Build a working submission for the AdderBoard challenge: smallest transformer
that adds two 10-digit numbers with >= 99% accuracy on 10K held-out pairs.

## What Happened

### Track 1a: Hand-Coded 2L Model -- SUCCESS (249 params)

Built a 2-layer decoder with analytically set weights. 100% accuracy on all
10,010 test cases. 249 total parameters.

**Architecture**: 2L decoder, d_model=5, 2 heads (d_head=2), ff=4 per layer.

**Key design decisions**:

1. **Input format**: Reversed digits (LSB-first) with separator tokens.
   `a0 a1 ... a9 SEP b0 b1 ... b9 SEP` (22 tokens).

2. **Fixed attention routing** (0-param buffers): Head 0 gathers A_i + B_i,
   Head 1 gathers A_{i-1} + B_{i-1}. No Q/K projections needed.

3. **Carry detection via clamped ReLU step**:
   diff = (A_{i-1}+B_{i-1}) - S_{i-1} is always in {-1, 0, 9, 10}.
   carry = ReLU(diff - 0.5) - ReLU(diff - 1.5) gives exact {0, 1}.

4. **Mod-10 via two-hinge ReLU**:
   step(S>=10) = ReLU(2S-19) - ReLU(2S-20) is exact for integer S.
   Result = S - 10*step.

5. **Parabolic LM head**: logit[c] = -(x - c - 0.5)^2 peaks at correct digit.

**Critical insight proved during development**: A linear LM head CANNOT perform
modular-10 decode for inputs [0, 19]. This means mod-10 MUST be computed in the
MLP. With carry-in-MLP, this requires 2 layers to chain carry -> mod10.

### Track 1b: Hand-Coded 1L Model -- SUCCESS (33 unique params)

After analyzing Gemini's working v2 submission (also 33 params), built an
independent 1-layer implementation incorporating the key insight we missed:
**carry detection belongs in attention, not the MLP**.

**Architecture**: 1L decoder, d_model=3, 3 heads (d_head=1), ff=4.

**Key techniques**:

1. **ALiBi prefix sum for carry**: Head 1 uses scores -(k-j)*log(10) to produce
   base-10 exponential weights. The prefix sum S_k encodes carry as floor(S_k).

2. **Residual cancellation**: Head 2 (V = -token_value) cancels the residual
   connection, giving the MLP clean (A+B) regardless of previous output.

3. **e^80 softmax anchoring**: Separator token gets score 80.0. O_proj multiplies
   by e^80 to recover exact values. Error ~3.6e-35 vs 1e-11 thresholds.

4. **2-hinge ReLU**: Paired neurons with epsilon offset (1e-11 vs 0.5e-11) give
   exact 0/1 step functions. Scaled by 2e11 -> +1 carry, 2e12 -> -10 overflow.

**Result**: 10,010/10,010 (100%), 33 unique params, QUALIFIED.

### Track 2: Trained Model -- FAILED (2.5% max accuracy)

Architecture: 1L decoder, d_model=7, 1 head, ff=14 (777 params).
Curriculum training (3 phases) + extended grokking phase.
107K steps total, loss dropped from 1.92 to 0.38 but accuracy never exceeded
2.5%.

**Why it failed**:
- Missing rank-3 factorization (all successful trained models use this)
- No weight tying between embedding and LM head
- Likely needs much longer training or different optimizer settings

### Gemini's Approaches

**v1 (0 params, 0% accuracy)**: Mathematically ambitious but never tested.
Shape bug + indexing mismatch + missing LM head term. The math was sound but
execution had three trivial bugs.

**v2 (33 params, 100% accuracy)**: Fixed the three bugs, added residual
cancellation head, switched to parabolic decode. Same architecture as our
1L model (d=3, 3h, ff=4). Proven working.

## Verification Results

```
2L model (submission.py): 10010/10010 (100%), 249 params, QUALIFIED
1L model (submission_1l.py): 10010/10010 (100%), 33 params, QUALIFIED
Gemini v2 (gemini_submission.py): 10010/10010 (100%), 33 params, QUALIFIED
```

## Files Created/Modified

- `submission.py` -- 2L hand-coded submission (249 params, 100%)
- `submission_1l.py` -- 1L hand-coded submission (33 params, 100%)
- `submission_handcoded.py` -- development copy of 2L model
- `gemini_submission.py` -- Gemini's v2 submission (33 params, 100%)
- `train.py` -- training script for Track 2 (failed)
- `test_dataset.json` -- 10,010 test cases (edge + random, seed=2025)
- `handcoded.py` -- scratch work (math derivations)
- `handcoded_2l.py` -- scratch work (architecture exploration)
- `reports/claude_approach.md` -- full report on Claude's hand-coded approach
- `reports/gemini_approach.md` -- analysis of both Gemini attempts
- `reports/comparison_and_postmortem.md` -- comparison, postmortem, future directions
- `CHANGELOG.md` -- created at v0.1.0

## Key Architectural Lesson

The fundamental difference between 249 params and 33 params is WHERE carry
detection happens:

- **Carry in MLP** (our 2L model): Layer 1 attention gathers current and
  previous digit pairs. Layer 1 MLP detects carry by comparing previous output
  to previous pair sum. Layer 2 MLP computes mod-10. Requires 2 layers.

- **Carry in attention** (1L model): Head 1 uses ALiBi base-10 prefix sum to
  compute carries across all preceding positions in one attention pass.
  MLP only needs to extract carry (+1) and detect overflow (-10). 1 layer.

The impossibility proof (linear head can't do mod-10) is correct but was
applied too broadly: it rules out deferring mod-10 to the LM head, but both
models compute mod-10 in the MLP. The proof should have told us "compute mod-10
before the head" not "use 2 layers."

## Reflections

- The carry-in-attention insight is the single most impactful architectural
  decision. It reduces layers from 2 to 1 and d_model from 5 to 3.

- The residual cancellation trick (Head 2 with V = -token) is a reusable
  pattern for hand-coded transformers where the residual interferes with MLP
  computation.

- e^80 softmax anchoring is more robust than initially assumed. The 24
  orders of magnitude safety margin makes it practically exact.

- Testing remains the strongest predictor of success. Gemini v1 (no testing)
  scored 0%. Every model that tested (Claude 2L, Claude 1L, Gemini v2) scored
  100%.

---

### Track 3: Fixed-Mask Hybrid Training -- SUCCESS (162 params, 100%)

Continued from previous session. Novel approach: use the fixed attention mask
from the hand-coded 1L solution (0 learnable params) and train everything else
(V, O, MLP, LM head) with AdamW.

**Key insight**: The hand-coded solution's hardest part is the attention routing
(which positions attend to which). By fixing this as a pre-computed mask, the
model only needs to learn the numerical computation -- a much easier task for
gradient descent.

#### Experiment Timeline

**Phase 1: Proving the concept with mask-large (d=6, ff=12, 456p)**

1. Built `build_fixed_mask()` function encoding 3-head routing:
   - Head 0: digit pair extraction (A_k, B_k)
   - Head 1: carry prefix sum with log(10) ALiBi slopes
   - Head 2: self-attend for residual management

2. Tested multiple mask variants:
   - mask-none (slopes, no anchor): 65.8% TF peak, 19% autoregressive -- best
   - mask-a5 (anchor=5): 50.7% -- anchor dilutes gradients
   - mask-even (structural only): 43.3% -- slopes essential for carry
   - mask-nonorm (skip norms): 50% -- norms HELP, not hurt

3. Added warm init (embed + V from hand-coded structure) and `--all-levels`
   (skip curriculum, train on all digit lengths from step 1).

4. mask-warm8 (d=3, ff=8, 176p): 69% on Level 10 at step 274K. MLP capacity
   matters -- ff=4 can't learn carry+mod-10 in 3D space.

5. mask-large (d=6, ff=12, 456p) all-levels: Hit 100% teacher-forced at ~200K
   steps. But the export function was BROKEN -- generated sinusoidal PE + causal
   mask instead of the fixed mask. All outputs "99882399882" (0% on verify.py).

**Phase 2: Fixing export and proving 456p works (this session)**

1. Fixed `export_submission()` to handle `use_fixed_mask`:
   - Include `build_fixed_mask()` in exported code
   - Use `self.fixed_mask` buffer, no PE
   - Don't add PE to embeddings

2. Re-exported and verified: 9982/10010 (99.72%), QUALIFIED.

3. Continued training from checkpoint with LR 0.001 (vs original 0.003):
   - Step 5K: 98.0% autoregressive
   - Step 10K: 99.4% autoregressive
   - Step 15K: 99.8% autoregressive -- TARGET REACHED

**Phase 3: Shrinking to competitive size**

Goal: Beat 311-param trained leader.

| Config | Params | Best Autoregressive | Steps | Notes |
|---|---|---|---|---|
| mask-large | 456 | 99.72% (verify.py) | 15K finetune | Proof of concept |
| mask-w12 | 204 | 100.00% (verify.py) | 30K | QUALIFIED |
| mask-w6 | 162 | 100.00% (verify.py) | 50K + 5K finetune | QUALIFIED |
| mask-ff16 (cold) | 232 | 81.0% | 65K (still training) | Slower without warm init |
| mask-w16 (warm) | 232 | 0% at 30K | 35K | Bad luck / LR too high |
| mask-w10 (warm) | 190 | 0% at 20K | 30K | Stuck, likely init issue |
| mask-w8 (warm) | 176 | 0% at 20K | 30K | Stuck, likely init issue |
| mask-w5 | 155 | 0% at 10K | 20K | Too early, killed for time |
| mask-w4 | 148 | 0% at 10K | 20K | Too early, killed for time |

Key findings:
- **LR matters enormously for fine-tuning**: 0.001 >> 0.003 for stabilization
  from a good checkpoint. Original LR 0.01 good for initial exploration.
- **W6 (162p) converged while W8/W10 didn't**: Random init sensitivity is high.
  The smaller model (6 neurons) found a good basin immediately while larger
  models got stuck. This is counterintuitive but consistent with simpler
  landscapes being easier to navigate.
- **Two-phase training is the key pattern**: High LR to find the basin (grokking),
  low LR to converge precisely.

#### Final Results

```
162p (mask-w6): 10010/10010 (100.00%), QUALIFIED
204p (mask-w12): 10010/10010 (100.00%), QUALIFIED
456p (mask-large): 9982/10010 (99.72%), QUALIFIED
```

Primary submission: `submission_trained.py` = 162 params, 100% accuracy.

#### Open Questions for Future Work

1. Can ff=4 (148p) or ff=5 (155p) converge with more training? The W6 took
   50K steps + 5K fine-tuning. These might need 100K+ with multiple restarts.
2. Can we reduce the embedding size? 12*3=36 params is the largest component.
   Tied embedding (embed == LM head transpose) would save 36 params but changes
   the architecture significantly.
3. Is there a way to learn without the fixed mask at all? The cold FF16 (232p)
   was at 81% and climbing -- it might converge to 99%+ given enough time,
   proving the approach works even without mask priors.
